# Blender API stubs for autocomplete and type checking
fake-bpy-module-latest

# Optional: Code formatting and linting tools
black
pylint

# Optional: Type checking
mypy

# PDD 2.0: YAML parsing for build manifest
PyYAML